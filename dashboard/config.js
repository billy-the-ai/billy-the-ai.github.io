// Billy's dashboard API — permanent address on the Google Cloud server.
// Unlike the old tunnel, this URL does not change, so the Discord redirect
// and this file are set once.
window.BILLY_DASHBOARD_API_URL = "https://billyserverai.duckdns.org";
